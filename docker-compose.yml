version: "3"

services:
  cameleer:
    image: cameleer
    build: .
    user: $USER_ID:$GROUP_ID
    container_name: cameleer
    environment:
        - DISPLAY=${DISPLAY}
    volumes:
      - ./:/home/cameleer_with_docker
      - /tmp/.X11-unix:/tmp/.X11-unix
    network_mode: host
    tty: true
